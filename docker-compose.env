# Docker Compose environment variables
# Copy this file to .env and adjust values for production.

# Postgres credentials (shared by all services)
POSTGRES_USER=langchain
POSTGRES_PASSWORD=langchain
POSTGRES_DB=langchain

# JWT secret for the Node.js auth server — CHANGE THIS IN PRODUCTION
JWT_SECRET=ragmeup-jwt-secret-change-me-in-production

# Host port where the React UI is accessible
HOST_PORT=3000

# ---------------------------------------------------------------------------
# Hybrid mode only (docker-compose.hybrid.yml)
# Used when running the Python server standalone for GPU support.
# ---------------------------------------------------------------------------

# URL the Node server uses to reach the Python RAG server
# Default uses Docker's magic hostname that resolves to the host machine.
# PYTHON_SERVER_URL=http://host.docker.internal:5000

# Host port to expose Postgres on (so the standalone Python server can reach it)
# POSTGRES_PORT=6024
