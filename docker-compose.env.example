# Docker Compose environment variables
# Copy this file to .env and adjust values for production.

# Postgres credentials (shared by all services)
POSTGRES_USER=langchain
POSTGRES_PASSWORD=langchain
POSTGRES_DB=langchain

# JWT secret for the Node.js auth server — CHANGE THIS IN PRODUCTION
JWT_SECRET=ragmeup-jwt-secret-change-me-in-production

# Host port where the React UI is accessible
HOST_PORT=80
